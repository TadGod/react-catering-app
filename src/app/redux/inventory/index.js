import * as actions from './actions';
import * as actionTypes from './actionTypes';
import * as selectors from './selectors';

const inventory = { actions, actionTypes, selectors };

export default inventory;